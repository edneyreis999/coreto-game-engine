/**
 * Canonical Project Entity Definitions
 */

export interface ProjectInfo {
  path: string;
  name: string;
  isValid: boolean;
  troopsCount: number;
  classesCount: number;
  enemiesCount: number;
}

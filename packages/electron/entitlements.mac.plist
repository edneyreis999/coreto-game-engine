<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
  <!-- Allow file access to user-selected files -->
  <key>com.apple.security.files.user-selected.read-only</key>
  <true/>

  <!-- Allow file access to user-selected files for read-write -->
  <key>com.apple.security.files.user-selected.read-write</key>
  <true/>

  <!-- Allow outgoing network connections for future features -->
  <key>com.apple.security.network.client</key>
  <true/>

  <!-- Disable incoming network connections (not needed) -->
  <key>com.apple.security.network.server</key>
  <false/>

  <!-- Allow accessing local SQLite database files -->
  <key>com.apple.security.files.downloads.read-write</key>
  <true/>

  <!-- Enable JIT for JavaScript engine (required for Electron) -->
  <key>com.apple.security.cs.allow-jit</key>
  <true/>

  <!-- Enable loading of unsigned code (required for Electron) -->
  <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
  <true/>

  <!-- Allow execution of code from memory (required for V8) -->
  <key>com.apple.security.cs.allow-dyld-environment-variables</key>
  <true/>

  <!-- Disable library validation (required for Electron) -->
  <key>com.apple.security.cs.disable-library-validation</key>
  <true/>
</dict>
</plist>
